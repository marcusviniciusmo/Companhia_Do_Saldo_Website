import { ImageProps } from "../Global";

export interface SupplierCardMocks {
  buttonText: string;
};

export interface SupplierCardProps {
  id: number;
  image: ImageProps;
  title: string;
  text: string;
};
